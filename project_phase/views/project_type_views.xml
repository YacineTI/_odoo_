<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_project_project_type_form" model="ir.ui.view">
        <field name="name">view.project.project.type.form</field>
        <field name="model">project.project.type</field>
        <field name="arch" type="xml">
            <form string="Project type">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_type"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-pencil-square-o">
                            <field name="project_count"
                                   widget="statinfo"
                                   string="Project"/>
                        </button>

                        <button name="toggle_active" type="object"
                                class="oe_stat_button"
                                icon="fa-archive">
                            <field name="active"
                                   widget="boolean_button"
                                   options='{"terminology": "archive"}'/>
                        </button>
                    </div>

                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Name"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="code"/>
                            <field name="sequence" invisible="1"/>
                            <field name="company_id"
                                   options="{'no_create_edit': True,
                               'no_create':True,
                               'no_open':True}"
                            />
                        </group>
                        <group>
                            <field name="project_id"
                                   widget="many2many_tags"
                                   domain="[('project_type_id' ,'=', False)]"
                                   invisible="1"/>
                        </group>
                    </group>

                    <group string="Internal Notes">
                        <field name="note" nolabel="1"
                               placeholder="This note is only for internal purposes."/>
                    </group>

                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"
                           widget="mail_followers"/>
                    <field name="activity_ids"
                           widget="mail_activity"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
            </form>
        </field>
    </record>

    <record id="view_project_project_type_tree" model="ir.ui.view">
        <field name="name">view.project.project.type.tree</field>
        <field name="model">project.project.type</field>
        <field name="arch" type="xml">
            <tree string="Project type">
                <field name="sequence" widget="handle"/>
                <field name="project_id" invisible="1"/>
                <field name="code"/>
                <field name="name"/>
                <field name="note"/>
                <field name="company_id"
                       groups="base.group_multi_company"/>
                <field name="active" invisible="1"/>
            </tree>
        </field>
    </record>

    <record id="action_project_project_type_view"
            model="ir.actions.act_window">
        <field name="name">Project type</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">project.project.type</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">

            </p>
            <p>

            </p>
        </field>
    </record>

</odoo>

